# Reusable step template for installing Python dependencies
parameters:
  packages: 'sigma-cli ruamel.yaml pyyaml'

steps:
  - script: |
      python -m pip install --upgrade pip
      pip install ${{ parameters.packages }}
    displayName: 'Install Python dependencies'
