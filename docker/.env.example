# Splunk Test Environment Configuration
# Copy this file to .env and set your values

# Splunk admin password (minimum 8 characters)
SPLUNK_PASSWORD=changeme

# HEC (HTTP Event Collector) token for ingesting test data
SPLUNK_HEC_TOKEN=changeme
